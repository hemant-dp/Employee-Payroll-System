apiVersion: apps/v1
kind: Deployment
metadata:
  name: employee-app-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: employee-payroll
  template:
    metadata:
      labels:
        app: employee-payroll
    spec:
      containers:
        - name: payroll-container
          image: hrajpoot121/employee-backend:latest
          imagePullPolicy: Always # Critical for local images
          ports:
            - containerPort: 8081